# IS218 Test 1 - Calculator Project

This project implements a modular Python calculator with unit test coverage using `pytest`.

## Features
- Addition logic (`calculator/addition.py`) with unit tests (`tests/test_addition.py`)
- Subtraction logic (`calculator/subtraction.py`) with unit tests (`tests/test_subtraction.py`)